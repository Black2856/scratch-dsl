# 上流 Scratch 公式リポジトリの固定 checkout (read-only の調査用、各自の git 管理下)
/scratch-*/

# Node / Playwright (Phase 3 で導入)
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
# esbuild が生成するブラウザ用バンドル
/tests/e2e/.bundle/

# Local debug output
/debug.log

# Local fixture and test assets (kept on disk, not committed)
/workspace/

# Real-world .sb3 files dropped at the repo root for import checks (large, not source)
/*.sb3
